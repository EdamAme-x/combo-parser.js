{
  "tasks": {
    "dev": "deno run -A --unstable --watch main.ts",
    "start": "deno run -A --unstable main.ts",
    "url": "deno run -A --unstable url_counter.ts",
    "build": "deno compile -A --unstable main.ts"
  }
}
